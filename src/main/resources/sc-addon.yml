# This configuration file defines the server environment required to run this set of configurations
# version is the version requirement, at least the specified version is required to run
# version.minecraft fill in the major MC version, e.g. 1.20 -> 1.20

version:
  minecraft: 16
depend:
  - Slimefun
